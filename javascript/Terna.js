const age = 22;
const output = age>=18 ? "Eligible to vote" : "Not Eligible";
console.log(output);

// Output: Eligible to vote
// Explanation: The code checks if the age is 18 or more, and sets the status accordingly.

